<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title> {{ t }} </title>
</head>
<body>
    <section class="antialiased bg-gray-100 text-gray-600 h-screen px-4">
        <div class="flex flex-col  h-full">
            <!-- Table -->
            <div class="w-full max-w-2xl mx-auto bg-white shadow-lg rounded-sm border border-gray-200">
                <header class="px-5 py-4 border-b border-gray-100">
                    <h2 class="font-semibold text-gray-800">Utilisateur</h2>
                </header>
                <div class="p-3">
                    <div class="overflow-x-auto">
                        <table class="table-auto w-full">
                            <thead class="text-xs font-semibold uppercase text-gray-400 bg-gray-50">
                            <tr>
                                <th class="p-2 whitespace-nowrap">
                                    <div class="font-semibold text-left">Pseudo</div>
                                </th>
                                <th class="p-2 whitespace-nowrap">
                                    <div class="font-semibold text-left">Email</div>
                                </th>
                                {% if role == 2 %}
                                <th class="p-2 whitespace-nowrap">
                                    <div class="font-semibold text-left">Role</div>
                                </th>
                                <th class="p-2 whitespace-nowrap">
                                    <div class="font-semibold text-center">Administration </div>
                                </th>
                                {% endif %}
                            </tr>
                            </thead>
                            <tbody class="text-sm divide-y divide-gray-100">
{% for user in users %}
                            <tr>

                                <td class="p-2 whitespace-nowrap">
                                    <div class="flex items-center">
                                        <a href="http://localhost/Unlinkedout/user/{{user.id}}">
                                            <div class="w-7 h-13 flex-shrink-0 mr-2 sm:mr-3"><img class="rounded-full" src="http://localhost/Unlinkedout/{{ user.avatar }}" width="40" height="40"></div>
                                        </a>
                                        <div class="font-medium text-gray-800">{{ user.name }}</div>
                                    </div>
                                </td>
                                <td class="p-2 whitespace-nowrap">
                                    <div class="text-left">{{user.email}}</div>
                                </td>
                                {% if role == 2 %}
                                <td class="p-2 whitespace-nowrap">
                                    {% if user.role == 2 %}
                                        <div class="text-left font-medium text-red-900">admin</div>
                                    {% endif %}
                                    {% if user.role == 1 %}
                                        <div class="text-left font-medium text-green-900">validate</div>
                                    {% endif %}
                                    {% if user.role == 0 %}
                                        <div class="text-left font-medium text-blue-900">inactif</div>
                                    {% endif %}
                                </td>
                                <td class="p-2 whitespace-nowrap">
                                    <div class=" text-center">
                                        <a class="text-blue-900" href="http://localhost/Unlinkedout/user/all/{{user.id}}/inactif"> inactif  </a>
                                        <a  class="text-green-900"href="http://localhost/Unlinkedout/user/all/{{user.id}}/validate"> validate  </a>
                                        <a  class="text-red-900" href="http://localhost/Unlinkedout/user/all/{{user.id}}/admin"> admin  </a>
                                    </div>
                                </td>
                            </tr>
                                {% endif %}
{% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
        <p> <a href="http://localhost/Unlinkedout/post/{{users.id}}">{{ users.title }} </p>
       {{ user.email }}  </a>

            son role est {{ user.role }}
             <a href="http://localhost/Unlinkedout/user/all/{{user.id}}/inactif"> inactif  </a>
             <a href="http://localhost/Unlinkedout/user/all/{{user.id}}/validate"> validate  </a>
             <a href="http://localhost/Unlinkedout/user/all/{{user.id}}/admin"> admin  </a>

</body>
{% if role == 2 %}
    <a href="http://localhost/Unlinkedout/user/all/tovalidate"> à valider  </a>
    <a href="http://localhost/Unlinkedout/user/all"> / tout  </a>
{% endif %}
<footer>

</footer>
</html>
